# WARNING: This file contains placeholder environment variables.
#
# Do NOT paste real secrets into a public repository. Storing API keys,
# passwords, or other secrets in version control is insecure.
#
# Note: Render does NOT automatically import a `.env` file from your
# repository. To make env vars available to the running service on Render,
# add them in the Render dashboard (Service -> Environment) and redeploy.
#
# This `.env` is provided as a convenience so you can test locally or
# copy/paste values into Render. If you *do* intend to commit this file,
# remove secrets beforehand or use `.env.example` instead.

### Basic app settings
DATABASE_URL=postgres://<DB_USER>:<DB_PASS>@<DB_HOST>:<DB_PORT>/<DB_NAME>
JWT_SECRET=replace_with_a_strong_jwt_secret
PORT=10000

### Email / SMTP settings (replace placeholders with real values)
# Recommended: SendGrid example
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=REPLACE_WITH_SENDGRID_API_KEY
SMTP_SECURE=false
EMAIL_FROM=no-reply@yourdomain.com

# Enable to log OTPs to stdout instead of actually sending mail (useful for testing)
DEBUG_EMAIL_MODE=false

# OTP settings
OTP_EXPIRATION_MINUTES=10
OTP_RATE_LIMIT_PER_HOUR=5
# Database Credentials (Must match your PostgreSQL setup)
DB_USER=clipboard_user
DB_PASSWORD="2346"  # The exact password you set with ALTER ROLE
DB_NAME=secure_clipboard_db
DB_HOST=localhost
DB_PORT=5432

# JWT Secret Key - MUST BE LONG AND RANDOM!
JWT_SECRET=48e8d8e44ea85968c550c07cd7aa5fa0bf86c5ed5cd367619dbcabdb5ebb7c1a251f39108bf6954f718099234defacce562d63a1d1d58bf5d4851ce6dc752bb7
VITE_API_URL=https://universal-clipboard-q6po.onrender.com/api/auth
VITE_API_BASE_URL=https://universal-clipboard-q6po.onrender.com/api/clipboard